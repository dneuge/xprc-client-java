package de.energiequant.xprc.commands;

import de.energiequant.xprc.ChannelMessage;

public class CMHDMessage extends ChannelMessage {
    CMHDMessage(ChannelMessage msg) {
        super(msg);
    }
}
